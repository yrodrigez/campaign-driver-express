import notFound from "./not-found.js";

const adobeCampaignControllers = Object.freeze({})
const appControllers = Object.freeze({
  notFound,
  ...adobeCampaignControllers
})

export default appControllers